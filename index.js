// 1. divide function expression
const divide = function() {
    return 2000 / 100;
  };
  
  // 2. square arrow function
  const square = (x) => x * x;
  
  // 3. add arrow function
  const add = (a, b) => a + b;
  